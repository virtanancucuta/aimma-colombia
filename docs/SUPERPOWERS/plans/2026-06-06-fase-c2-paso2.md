# Fase C.2 · Paso 2 — Edición inline de texto simple · Plan por fases

> SUB-SKILL: subagent-driven-development. Pasos checkbox. **Diseño aprobado en el gate antes de implementar.**

**Goal:** editar texto simple inline en el lienzo (commit por la misma ruta del inspector; rich-text sigue por inspector).
**Architecture:** edición en el iframe (contenteditable, preview-gated, self-marking `data-ed-field`); commit→admin por postMessage G3; suspend de patches de la sección en edición (spike validado); dormido por `inline-enable`.
**Spec:** `docs/SUPERPOWERS/specs/2026-06-06-fase-c2-paso2-design.md`.

---

## GATE 0 — (sin rotación)
Paso 2 no expone secretos nuevos. La rotación Supabase Track 2+3 + cleanup del PAT read-only de GitHub siguen en backlog (no bloquean). Regla vigente: secretos por env/stdin, nunca argv.

## FASE A — Storefront (deploy PRIMERO · dormido por `inline-enable`)
- [ ] **A1 — `Astro.locals.isPreview`**: setearlo en `index.astro` (según token) y `render-fragment.astro` (=true). Test: locals llega a los renderers.
- [ ] **A2 — Registro + marcadores**: `SIMPLE_TEXT_FIELDS[tipo]` + `setByPath/getByPath` (paquete compartido). Renderers de texto-simple emiten `data-ed-field="<ruta>"` SOLO con `locals.isPreview`. Test SSR: presente en preview, **ausente** en público; **paridad render-fragment byte-idéntica** (preview-vs-preview) sin regresión.
- [ ] **A3 — `InlineEdit.astro`** (is:inline, `{isPreview}`): lifecycle contenteditable — activar el nodo `data-ed-field` de la sección seleccionada SOLO tras `inline-enable`; Enter(1 línea)/blur/click-afuera→commit; Escape→revert; paste→texto plano; postMessage `inline-edit-start`/`inline-commit`/`inline-cancel`. Incluir en `index.astro` bajo `{isPreview}`.
- [ ] **A4 — Suite + build** storefront verde.
- [ ] **A5 [GATE deploy]**: `wrangler deploy` → **byte-compare público** (`data-ed-field` ausente, cuerpo sin cambios, caracterizar CSS como 7a) → **gate dormido** (con admin viejo: inline-edit NO se activa porque no llega `inline-enable`; C.1/C.2-Paso1 sin regresión). Reportar. **OK de Jorge → Fase B.**
  - *Rollback*: `wrangler rollback` a la versión previa.

## FASE B — Admin (merge a main → Easypanel)
- [ ] **B1 — `editor-canvas.js`**: enviar `inline-enable` al montar el iframe; handlers `inline-edit-start/commit/cancel` con G3 (origin + sección conocida + `fieldPath ∈ SIMPLE_TEXT_FIELDS[tipo]` + value string ≤ cap) ANTES de mutar; commit → `updateSectionPropByPath`. Tests: rechazos G3 + despacho.
- [ ] **B2 — `editor.js` suspend**: el carril patch saltea patches de `editingField.sectionId` mientras dura la edición; saltea el patch del commit; limpia `editingField` tras drenar. **Hotfix autosave intacto.** Test: patch a la sección en edición se saltea; otras secciones no.
- [ ] **B3 — `setByPath` + registro** (mismo que storefront) + `updateSectionPropByPath`. Tests setByPath (simple/anidado/array).
- [ ] **B4 — Suite admin verde** (incl. nuevos; sin regresión Paso 1).
- [ ] **B5 [GATE deploy]**: confirmar diff → merge `--no-ff` a main → **Jorge redeploya Easypanel** → curl markers nuevos. *Rollback*: revert del merge + redeploy.

## FASE C — Verificación
- [ ] **C1 — Adversarial chromium-real** (foco inline-commit): sanity positivo primero; origin forjado / fieldPath desconocido / sección desconocida → no mutan; value no-string → ignorado.
- [ ] **C2 [GATE final] — Live**: inline edit un título → typing → commit (blur/Enter) → guardado (estado+DB) + **sin clobber + cursor preservado**; patch mid-edit **suspendido**; Escape revierte; paste plano; **HOTFIX sobrevive** (persiste/F5/401 visible); chrome Paso 1 sigue; **rich-text/multilínea sigue SOLO inspector** (no inline). Restaurar aimma-test. Reportar resultados concretos.

## Refinamientos del gate (aplican a las tareas de arriba)
- **A2**: `data-ed-field` IDÉNTICO en index-preview Y render-fragment (verificar byte-a-byte que el nodo patcheado se auto-marca igual).
- **A3**: captura = `textContent` LIMPIADO (br espurio / `&nbsp;` / spans-divs del contenteditable) → texto plano limpio.
- **A5**: gate dormido = marcadores presentes-pero-inertes (sin `inline-enable` no hay contenteditable) + C.1 patch andando + público sin marcadores.
- **B2**: SUSPEND con AUTO-RECUPERACIÓN (timeout ~20s y/o on-next-interaction: cambio de selección / nuevo start de otra sección) — nunca congelado silencioso. Hotfix intacto.
- **B3**: `setByPath` guardea `__proto__`/`constructor`/`prototype` (defensa en profundidad además del allowlist).
- **C2**: + patch POSTERIOR al commit snap-ea al valor limpio (sin doblar/perder); + tras reload el campo byte-matchea el SSR (paridad nodo); adversarial cubre los 4 mensajes (origin/enum/sección/fieldPath-fuera-registro/value-no-string rechazados antes de mutar).

## Self-review (cobertura)
- Q1 iframe-side → A3/InlineEdit. Q2 marcador isPreview self-marking → A1/A2. Q3 suspend (spike) → B2. Q4 commit/cancel/paste → A3. Q5 misma ruta + texto plano → B1/B3. Q-dormido inline-enable → A3/B1/A5. No-negociables (preview-gated/G3/A3/reuso/hotfix) → A2/B1/B2/A5/C2.

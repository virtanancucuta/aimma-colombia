# Rich-text (B-controles #4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir `texto.contenido` en rich-text con sanitización autoritativa en la EF (`sanitize-html`) + defensa en profundidad en el storefront (`DOMPurify`), gobernadas por un allowlist canónico único.

**Architecture:** Política canónica lib-agnóstica (`packages/database/src/richtext-policy.ts`) con 2 adaptadores (toSanitizeHtml / toDOMPurify). EF mirror byte-idéntico + admin mirror JS, ambos sync-testeados. EF hace sanitize-and-store; storefront renderea con `set:html`. Zod NO cambia. Spike ya confirmó `sanitize-html@2.13.1` en el Deno real (isomorphic-dompurify/linkedom/happy-dom descartados).

**Tech Stack:** sanitize-html@2.13.1 (EF, Deno via deno.json import map), isomorphic-dompurify@2.36.0 / dompurify@3.4.7 (storefront + admin), Astro 5, vitest (golden), node --import tsx --test (tests/editor).

**Branch:** `fase-b-richtext` (ya creada, sobre main; spec en `docs/SUPERPOWERS/specs/2026-06-04-richtext-design.md` HEAD a603926).

**Gate de deploy (NO incluido en este plan — Jorge lo revisa aparte):** tras la última tarea, llevar a Jorge diff + resultados test seguridad dual + idempotencia verde + golden byte-a-byte. Sin eso, NO se deploya.

---

## File Structure

- **Create** `packages/database/src/richtext-policy.ts` — política canónica + 2 adaptadores (pura, sin imports de libs).
- **Modify** `packages/database/src/index.ts` — `export * from './richtext-policy';`.
- **Create** `supabase/functions/tienda-guardar-layout/richtext-policy.ts` — mirror byte-idéntico del canónico.
- **Modify** `supabase/functions/tienda-guardar-layout/deno.json` — agregar import `sanitize-html`.
- **Modify** `supabase/functions/tienda-guardar-layout/index.ts` — sanitize-and-store de `texto.contenido`.
- **Create** `iapanel/tienda/admin/views/editor/richtext-policy.js` — mirror JS browser de la política (valor-synceado).
- **Modify** `iapanel/tienda/admin/views/editor/editor-controls.js` — control `richText`.
- **Modify** `iapanel/tienda/admin/views/editor/editor-inspector.js` — `case 'richtext'`.
- **Modify** `iapanel/tienda/admin/views/editor/section-defs.js` — `texto.contenido` control `textarea`→`richtext`.
- **Modify** `iapanel/tienda/admin/index.html` — DOMPurify CDN + richtext-policy.js + bump `?v`.
- **Modify** `apps/storefront/package.json` — pin `isomorphic-dompurify` exacto `2.36.0`.
- **Modify** `apps/storefront/src/components/blocks/texto/Texto.astro` — `set:html` + dropcap EM→CSS.
- **Modify** `apps/storefront/test/texto.golden.test.ts` — fixture HTML rich-text.
- **Create** `tests/editor/10-richtext-policy-sync.test.mjs` — mirror byte-sync + admin value-sync + fidelidad adaptadores.
- **Create** `tests/editor/11-richtext-security.test.mjs` — test seguridad dual + idempotencia.
- **Modify** `tests/editor/package.json` — devDeps `sanitize-html@2.13.1` + `isomorphic-dompurify@2.36.0`.

---

## Task 1: Política canónica + 2 adaptadores

**Files:**
- Create: `packages/database/src/richtext-policy.ts`
- Modify: `packages/database/src/index.ts`
- Test: `tests/editor/10-richtext-policy-sync.test.mjs` (sub-test fidelidad; el archivo completo se crea en Task 3 — acá solo el primer sub-test)

- [ ] **Step 1: Crear la política canónica**

Create `packages/database/src/richtext-policy.ts`:

```ts
// AIMMA Editor PRO-MAX · B-controles #4 rich-text · Politica canonica lib-agnostica.
// UNA fuente de verdad para la allowlist. Dos adaptadores: sanitize-html (EF autoritativa)
// y DOMPurify (storefront defensa en profundidad + admin best-effort). Mirror byte-identico
// en supabase/functions/tienda-guardar-layout/richtext-policy.ts (sync-test 10).
// NO importa ninguna lib (datos + funciones puras) -> Deno-compatible + bundleable por Vite.

export const RICHTEXT_POLICY = {
  tags: ['b', 'strong', 'i', 'em', 'a', 'ul', 'ol', 'li', 'p', 'br'],
  attrs: { a: ['href'] } as Record<string, string[]>,
  schemes: ['https', 'mailto', 'tel'],
  allowProtocolRelative: false,
};

// Adaptador sanitize-html (EF). disallowedTagsMode 'discard' elimina tag + (script/style) su texto.
// selfClosing: [] -> void elements (<br>) se emiten SIN auto-cierre, igual que DOMPurify, para que
// el HTML guardado sea punto fijo de la DOMPurify del storefront (idempotencia, test 11).
export function toSanitizeHtml(policy = RICHTEXT_POLICY) {
  return {
    allowedTags: [...policy.tags],
    allowedAttributes: Object.fromEntries(
      Object.entries(policy.attrs).map(([k, v]) => [k, [...v]]),
    ),
    allowedSchemes: [...policy.schemes],
    allowProtocolRelative: policy.allowProtocolRelative,
    disallowedTagsMode: 'discard' as const,
    selfClosing: [] as string[],
  };
}

// Adaptador DOMPurify (storefront + admin). ALLOWED_ATTR es global; como href solo aplica a <a>
// y solo <a> esta en ALLOWED_TAGS, equivale al por-tag de sanitize-html. URI solo https/mailto/tel.
export function toDOMPurify(policy = RICHTEXT_POLICY) {
  const attrs = [...new Set(Object.values(policy.attrs).flat())];
  return {
    ALLOWED_TAGS: [...policy.tags],
    ALLOWED_ATTR: attrs,
    ALLOWED_URI_REGEXP: new RegExp('^(' + policy.schemes.join(':|') + ':)', 'i'),
    FORBID_TAGS: ['script', 'style', 'iframe', 'object', 'embed', 'form', 'input', 'svg', 'math'],
    FORBID_ATTR: ['style', 'class', 'id', 'target'],
    ALLOW_DATA_ATTR: false,
  };
}
```

- [ ] **Step 2: Re-exportar desde el índice del paquete**

In `packages/database/src/index.ts`, después de la línea `export * from './editor-schema';` (última línea), agregar:

```ts
export * from './richtext-policy';
```

- [ ] **Step 3: Escribir el test de fidelidad de adaptadores (falla primero)**

Create `tests/editor/10-richtext-policy-sync.test.mjs` con SOLO este test por ahora (los otros sub-tests se agregan en Task 2 y 3):

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { RICHTEXT_POLICY, toSanitizeHtml, toDOMPurify } from '../../packages/database/src/richtext-policy.ts';

// Fidelidad: ambos adaptadores derivan FIELMENTE del canonico -> atrapa drift en cualquiera.
test('policy-sync: toSanitizeHtml deriva del canonico', () => {
  const a = toSanitizeHtml(RICHTEXT_POLICY);
  assert.deepEqual(a.allowedTags, RICHTEXT_POLICY.tags, 'allowedTags != policy.tags');
  assert.deepEqual(a.allowedAttributes, RICHTEXT_POLICY.attrs, 'allowedAttributes != policy.attrs');
  assert.deepEqual(a.allowedSchemes, RICHTEXT_POLICY.schemes, 'allowedSchemes != policy.schemes');
  assert.equal(a.disallowedTagsMode, 'discard');
});

test('policy-sync: toDOMPurify deriva del canonico', () => {
  const d = toDOMPurify(RICHTEXT_POLICY);
  assert.deepEqual(d.ALLOWED_TAGS, RICHTEXT_POLICY.tags, 'ALLOWED_TAGS != policy.tags');
  assert.deepEqual(d.ALLOWED_ATTR, ['href'], 'ALLOWED_ATTR != uniq(flatten(attrs))');
  assert.equal(d.ALLOWED_URI_REGEXP.source, '^(https:|mailto:|tel:)', 'regex de schemes drifteo');
  assert.equal(d.ALLOWED_URI_REGEXP.flags, 'i');
  assert.equal(d.ALLOW_DATA_ATTR, false);
});
```

- [ ] **Step 4: Correr el test**

Run (desde `tests/editor/`): `npm test`
Expected: los 2 tests de policy-sync PASAN (el resto de la suite sigue verde). Si falla la resolución del `.ts`, confirmar que `tsx` está instalado (`npm install` en tests/editor).

- [ ] **Step 5: Commit**

```bash
git add packages/database/src/richtext-policy.ts packages/database/src/index.ts tests/editor/10-richtext-policy-sync.test.mjs
git commit -m "feat(richtext): politica canonica + adaptadores sanitize-html/DOMPurify + fidelidad"
```

---

## Task 2: Mirror de la política en la EF + sync-test byte-idéntico

**Files:**
- Create: `supabase/functions/tienda-guardar-layout/richtext-policy.ts`
- Modify: `tests/editor/10-richtext-policy-sync.test.mjs`

- [ ] **Step 1: Agregar el sync-test byte-idéntico (falla primero)**

En `tests/editor/10-richtext-policy-sync.test.mjs`, agregar al inicio los imports de fs/path y un test (modelado en 04-ef-schema-sync):

```js
import { readFileSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const HERE = dirname(fileURLToPath(import.meta.url));

test('policy-sync: EF richtext-policy.ts es mirror byte-identico del canonico', () => {
  const canonical = readFileSync(resolve(HERE, '../../packages/database/src/richtext-policy.ts'), 'utf8');
  const efCopy = readFileSync(resolve(HERE, '../../supabase/functions/tienda-guardar-layout/richtext-policy.ts'), 'utf8');
  assert.equal(efCopy, canonical,
    'la copia del EF drifteo. Re-sincronizar: cp packages/database/src/richtext-policy.ts supabase/functions/tienda-guardar-layout/richtext-policy.ts');
});
```

- [ ] **Step 2: Correr el test (debe fallar: no existe el mirror)**

Run (desde `tests/editor/`): `npm test`
Expected: FAIL en "EF richtext-policy.ts es mirror" (ENOENT — el archivo del EF no existe aún).

- [ ] **Step 3: Crear el mirror (copia byte-idéntica)**

Run (desde la raíz del repo):
```bash
cp packages/database/src/richtext-policy.ts supabase/functions/tienda-guardar-layout/richtext-policy.ts
```

- [ ] **Step 4: Correr el test (pasa)**

Run (desde `tests/editor/`): `npm test`
Expected: PASS el sync-test byte-idéntico + los de fidelidad.

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/tienda-guardar-layout/richtext-policy.ts tests/editor/10-richtext-policy-sync.test.mjs
git commit -m "feat(richtext): mirror byte-identico de la politica en la EF + sync-test"
```

---

## Task 3: Mirror JS de la política en el admin + value-sync test

**Files:**
- Create: `iapanel/tienda/admin/views/editor/richtext-policy.js`
- Modify: `tests/editor/10-richtext-policy-sync.test.mjs`

- [ ] **Step 1: Agregar el value-sync test del admin (falla primero)**

En `tests/editor/10-richtext-policy-sync.test.mjs`, agregar import del harness y un test:

```js
import { bootWindow } from './harness.mjs';

test('policy-sync: el mirror JS del admin coincide en valores con el canonico', () => {
  const win = bootWindow(['richtext-policy.js']);
  const P = win.TiendaIA.richtextPolicy;
  assert.ok(P, 'window.TiendaIA.richtextPolicy no existe');
  assert.deepEqual(P.POLICY.tags, RICHTEXT_POLICY.tags, 'tags del admin difieren');
  assert.deepEqual(P.POLICY.attrs, RICHTEXT_POLICY.attrs, 'attrs del admin difieren');
  assert.deepEqual(P.POLICY.schemes, RICHTEXT_POLICY.schemes, 'schemes del admin difieren');
  // El adaptador DOMPurify del admin == el canonico (campo por campo; RegExp por source/flags).
  const adm = P.toDOMPurify();
  const can = toDOMPurify(RICHTEXT_POLICY);
  assert.deepEqual(adm.ALLOWED_TAGS, can.ALLOWED_TAGS);
  assert.deepEqual(adm.ALLOWED_ATTR, can.ALLOWED_ATTR);
  assert.equal(adm.ALLOWED_URI_REGEXP.source, can.ALLOWED_URI_REGEXP.source);
  assert.equal(adm.ALLOWED_URI_REGEXP.flags, can.ALLOWED_URI_REGEXP.flags);
  assert.deepEqual(adm.FORBID_TAGS, can.FORBID_TAGS);
  assert.deepEqual(adm.FORBID_ATTR, can.FORBID_ATTR);
  assert.equal(adm.ALLOW_DATA_ATTR, can.ALLOW_DATA_ATTR);
});
```

- [ ] **Step 2: Correr el test (falla: no existe el mirror JS)**

Run (desde `tests/editor/`): `npm test`
Expected: FAIL (no se puede leer `richtext-policy.js` / `P` undefined).

- [ ] **Step 3: Crear el mirror JS del admin**

Create `iapanel/tienda/admin/views/editor/richtext-policy.js`:

```js
/* AIMMA Tienda IA · Editor PRO-MAX · B-controles #4 rich-text · richtext-policy.js v1
 * Mirror BROWSER de la politica canonica (packages/database/src/richtext-policy.ts).
 * Valor-synceado por tests/editor/10-richtext-policy-sync.test.mjs (no byte: distinta sintaxis).
 * Lo usa el control richText para normalizar el contenteditable con DOMPurify (CDN) -> WYSIWYG honesto.
 */
(function(window) {
  'use strict';
  var POLICY = {
    tags: ['b', 'strong', 'i', 'em', 'a', 'ul', 'ol', 'li', 'p', 'br'],
    attrs: { a: ['href'] },
    schemes: ['https', 'mailto', 'tel'],
    allowProtocolRelative: false,
  };
  function toDOMPurify(policy) {
    policy = policy || POLICY;
    var attrs = [];
    Object.keys(policy.attrs).forEach(function(k) {
      policy.attrs[k].forEach(function(a) { if (attrs.indexOf(a) === -1) attrs.push(a); });
    });
    return {
      ALLOWED_TAGS: policy.tags.slice(),
      ALLOWED_ATTR: attrs,
      ALLOWED_URI_REGEXP: new RegExp('^(' + policy.schemes.join(':|') + ':)', 'i'),
      FORBID_TAGS: ['script', 'style', 'iframe', 'object', 'embed', 'form', 'input', 'svg', 'math'],
      FORBID_ATTR: ['style', 'class', 'id', 'target'],
      ALLOW_DATA_ATTR: false,
    };
  }
  window.TiendaIA = window.TiendaIA || {};
  window.TiendaIA.richtextPolicy = { POLICY: POLICY, toDOMPurify: toDOMPurify };
})(window);
```

- [ ] **Step 4: Correr el test (pasa)**

Run (desde `tests/editor/`): `npm test`
Expected: PASS el value-sync del admin + los anteriores.

- [ ] **Step 5: Commit**

```bash
git add iapanel/tienda/admin/views/editor/richtext-policy.js tests/editor/10-richtext-policy-sync.test.mjs
git commit -m "feat(richtext): mirror JS de la politica en el admin + value-sync test"
```

---

## Task 4: Test de seguridad DUAL + idempotencia (la red de seguridad)

**Files:**
- Modify: `tests/editor/package.json`
- Create: `tests/editor/11-richtext-security.test.mjs`

- [ ] **Step 1: Agregar las libs al runner de tests**

En `tests/editor/package.json`, agregar a `devDependencies` (mantener orden alfabético):

```json
    "isomorphic-dompurify": "2.36.0",
    "jsdom": "^25.0.1",
    "sanitize-html": "2.13.1",
    "tsx": "^4.19.2",
    "zod": "3.25.76"
```

- [ ] **Step 2: Instalar**

Run (desde `tests/editor/`): `npm install`
Expected: instala sanitize-html@2.13.1 + isomorphic-dompurify@2.36.0 sin errores.

- [ ] **Step 3: Escribir el test de seguridad dual + idempotencia (falla primero si la política tuviera un hueco)**

Create `tests/editor/11-richtext-security.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import sanitizeHtml from 'sanitize-html';
import DOMPurify from 'isomorphic-dompurify';
import { RICHTEXT_POLICY, toSanitizeHtml, toDOMPurify } from '../../packages/database/src/richtext-policy.ts';

// EF (autoritativa) y storefront (defensa en profundidad) usan las MISMAS versiones que produccion
// (sanitize-html@2.13.1, isomorphic-dompurify@2.36.0/dompurify@3.4.7). Este test es la red que
// habria atrapado el passthrough de linkedom: prueba AMBAS capas por separado.
const SH = (html) => sanitizeHtml(html, toSanitizeHtml(RICHTEXT_POLICY));
const DP = (html) => DOMPurify.sanitize(html, toDOMPurify(RICHTEXT_POLICY));

const PAYLOADS = [
  '<script>alert(1)</script>',
  '<a href="javascript:alert(1)">x</a>',
  '<a href=" javascript:alert(1)">x</a>',
  '<img src=x onerror=alert(1)>',
  '<iframe src="https://evil"></iframe>',
  '<b onclick="alert(1)">x</b>',
  '<a href="data:text/html,<script>alert(1)</script>">x</a>',
  '<style>*{x}</style>',
  '<svg><script>alert(1)</script></svg>',
  '<svg onload=alert(1)>',
];

// Un output es seguro si no retiene tag/scheme/handler peligroso.
function assertSafe(out, label) {
  const low = out.toLowerCase();
  for (const bad of ['<script', '<iframe', '<svg', '<style', '<object', '<embed', '<form', '<input',
    'javascript:', 'data:text/html', 'onerror', 'onload', 'onclick']) {
    assert.ok(!low.includes(bad), `${label}: el output retiene "${bad}" -> ${out}`);
  }
}

for (const payload of PAYLOADS) {
  test(`seguridad EF (sanitize-html): ${payload}`, () => assertSafe(SH(payload), 'EF'));
  test(`seguridad storefront (DOMPurify): ${payload}`, () => assertSafe(DP(payload), 'SF'));
}

// Caso positivo: el contenido legitimo sobrevive en AMBAS capas.
const LEGIT = [
  '<b>hola</b> <a href="https://x.com">link</a> <ul><li>a</li><li>b</li></ul>',
  '<p>Parrafo uno.</p><p>Parrafo <strong>dos</strong> con <em>enfasis</em>.</p>',
  'Linea uno<br>Linea dos',
  '<a href="mailto:hola@aimma.com.co">correo</a> y <a href="tel:+573001112233">tel</a>',
];

for (const legit of LEGIT) {
  test(`positivo EF conserva formato: ${legit}`, () => {
    const out = SH(legit);
    assert.ok(/<(b|strong|em|a|ul|li|p|br)/i.test(out), `EF borro todo el formato: ${out}`);
  });
  // Idempotencia (correctitud): el storefront sobre el output de la EF es NO-OP en contenido legitimo
  // -> el formato que el usuario guardo (y vio guardado) no desaparece al renderear.
  test(`idempotencia DP(SH(x))===SH(x): ${legit}`, () => {
    const stored = SH(legit);
    assert.equal(DP(stored), stored, `el storefront altera el HTML almacenado legitimo`);
  });
  // Direccion complementaria (acuerdo admin/EF): SH sobre el output de DOMPurify tambien NO-OP.
  test(`idempotencia SH(DP(x))===DP(x): ${legit}`, () => {
    const norm = DP(legit);
    assert.equal(SH(norm), norm, `la EF altera el HTML que el admin/DOMPurify produjo`);
  });
}
```

- [ ] **Step 4: Correr el test**

Run (desde `tests/editor/`): `npm test`
Expected: PASS los de seguridad (la política es correcta; el spike ya lo verificó en sanitize-html). Si alguna **idempotencia** falla por formato de void-element (`<br>` vs `<br />`), el adaptador `toSanitizeHtml` ya fija `selfClosing: []` para emitir `<br>`; si aún diverge, ajustar SOLO el formato de salida de sanitize-html (NUNCA relajar tags/attrs/schemes) hasta que `DP(SH(x))===SH(x)`. La divergencia de seguridad NO es negociable; la de formato se resuelve alineando salida.

- [ ] **Step 5: Commit**

```bash
git add tests/editor/package.json tests/editor/package-lock.json tests/editor/11-richtext-security.test.mjs
git commit -m "test(richtext): seguridad dual (EF+storefront) + idempotencia sobre contenido legitimo"
```

---

## Task 5: EF sanitize-and-store

**Files:**
- Modify: `supabase/functions/tienda-guardar-layout/deno.json`
- Modify: `supabase/functions/tienda-guardar-layout/index.ts`

> Nota: la EF (Deno) no corre en la suite local; se verifica por inspección + (en el gate de deploy) por test de seguridad ya verde en Task 4 con la MISMA versión de sanitize-html. El deploy de la EF es parte del gate de Jorge, no de este plan.

- [ ] **Step 1: Agregar sanitize-html al import map de la EF**

En `supabase/functions/tienda-guardar-layout/deno.json`, dejar:

```json
{
  "imports": {
    "zod": "https://esm.sh/zod@3.25.76",
    "sanitize-html": "npm:sanitize-html@2.13.1"
  }
}
```

- [ ] **Step 2: Importar la lib + la política en index.ts**

En `supabase/functions/tienda-guardar-layout/index.ts`, tras la línea `import { PersonalizacionesSchema } from './editor-schema.ts';` (línea 16), agregar:

```ts
import sanitizeHtml from 'sanitize-html';
import { RICHTEXT_POLICY, toSanitizeHtml, normalizeVoidEls } from './richtext-policy.ts';
```

- [ ] **Step 3: Definir el helper sanitize-and-store**

En el mismo archivo, tras la constante `const MAX_PAYLOAD_BYTES = 2_000_000;` (línea 43), agregar:

```ts
const RICHTEXT_OPTS = toSanitizeHtml(RICHTEXT_POLICY);

// Sanitize-and-store AUTORITATIVO: limpia el HTML de cada seccion 'texto' antes de persistir.
// La BD nunca guarda HTML sucio, aunque alguien postee directo a la EF. Mutar in-place el home.
function sanitizeHome(home: any): void {
  if (!home || !Array.isArray(home.sections)) return;
  for (const sec of home.sections) {
    if (sec && sec.tipo === 'texto' && sec.props && typeof sec.props.contenido === 'string') {
      // normalizeVoidEls: <br /> -> <br> para que lo almacenado sea punto fijo de la DOMPurify del storefront.
      sec.props.contenido = normalizeVoidEls(sanitizeHtml(sec.props.contenido, RICHTEXT_OPTS));
    }
  }
}
```

- [ ] **Step 4: Llamar el helper tras la validación Zod**

En el mismo archivo, justo después del bloque `// 3) Zod validate` (después de la línea `}` que cierra el `try/catch` del parse, antes de `// 4) Ownership check`), agregar:

```ts
  // 3.5) Sanitizar HTML de secciones texto (autoritativo, antes de construir el JSON a guardar)
  sanitizeHome(body.personalizaciones.pages.home);
```

(`homeFromClient` en el paso 6 ya leerá la versión limpia, porque apunta al mismo objeto `body.personalizaciones.pages.home`.)

- [ ] **Step 5: Verificar sintaxis Deno (chequeo local de tipos/parseo)**

Run (desde la raíz): `npx --yes tsx --eval "import('./supabase/functions/tienda-guardar-layout/richtext-policy.ts').then(m=>console.log('policy ok', m.RICHTEXT_POLICY.tags.length))"`
Expected: `policy ok 10` (confirma que el mirror parsea como módulo; el index.ts usa imports Deno-only y NO se ejecuta acá — su verificación real es el deploy del gate).

- [ ] **Step 6: Commit**

```bash
git add supabase/functions/tienda-guardar-layout/deno.json supabase/functions/tienda-guardar-layout/index.ts
git commit -m "feat(richtext): EF sanitize-and-store de texto.contenido con sanitize-html"
```

---

## Task 6: Storefront — pin + set:html + dropcap EM→CSS + golden

**Files:**
- Modify: `apps/storefront/package.json`
- Modify: `apps/storefront/src/components/blocks/texto/Texto.astro`
- Modify: `apps/storefront/test/texto.golden.test.ts`
- Regenerate: `apps/storefront/test/__snapshots__/texto/*.html`

- [ ] **Step 1: Pin exacto de isomorphic-dompurify**

En `apps/storefront/package.json`, cambiar la dependencia:

```
    "isomorphic-dompurify": "2.36.0",
```

(de `"^2.20.0"` a `"2.36.0"` exacto). Luego correr (desde la raíz): `npm install`
Expected: el lockfile fija isomorphic-dompurify@2.36.0 + dompurify@3.4.7.

- [ ] **Step 2: Modificar Texto.astro — imports + sanitización + render**

En `apps/storefront/src/components/blocks/texto/Texto.astro`:

(a) Tras la línea `import SectionShell from '~/components/blocks/_SectionShell.astro';` (línea 12), agregar:

```ts
import DOMPurify from 'isomorphic-dompurify';
import { RICHTEXT_POLICY, toDOMPurify } from '@aimma/database';
```

(b) Borrar las líneas del dropcap por JS (líneas 35-38):

```ts
// editorial_magazine: dropcap solo en parrafos largos de lectura, alineados a la izquierda.
const conDropcap = plantilla === 'editorial_magazine' && !esDisplay && align === 'left' && p.contenido.trim().length > 0;
const firstLetter = conDropcap ? p.contenido.trimStart().charAt(0) : '';
const rest = conDropcap ? p.contenido.trimStart().slice(1) : p.contenido;
```

y reemplazarlas por:

```ts
// Rich-text: el contenido es HTML. Defensa en profundidad (la EF ya sanitiza al guardar).
const contenidoSafe = DOMPurify.sanitize(p.contenido, toDOMPurify(RICHTEXT_POLICY));
```

(c) Reemplazar el bloque del div con el ternario del dropcap (líneas 42-49) por un div con `set:html`:

```astro
  <div
    class={`${prefix}-texto ${fontClass} ${sizeClass} ${alignClass} text-[var(--ta-color-text-base)]`}
    data-field="contenido"
    set:html={contenidoSafe}
  />
```

- [ ] **Step 3: Dropcap EM por CSS — reemplazar la regla .em-dropcap**

En el mismo archivo, reemplazar el bloque `/* Dropcap editorial */ .em-dropcap { ... }` (líneas 148-159) por:

```css
  /* Dropcap editorial: primer caracter de parrafos de lectura (sm/md) alineados a la izquierda.
     Antes era un <span> recortado en JS; ahora ::first-letter (compatible con HTML rich-text). */
  .em-texto.block-text--size-sm.block-text--align-left::first-letter,
  .em-texto.block-text--size-md.block-text--align-left::first-letter {
    float: left;
    font-family: var(--ta-font-display);
    font-style: italic;
    font-weight: 300;
    color: var(--ta-color-accent);
    font-size: 3.4em;
    line-height: 0.78;
    padding-right: 0.08em;
    margin-top: 0.04em;
  }
```

- [ ] **Step 4: Agregar fixture HTML rich-text al golden**

En `apps/storefront/test/texto.golden.test.ts`, antes del cierre final del archivo (después del `describe('Texto unificado == snapshot', ...)` existente), agregar un segundo describe:

```ts
const CONTENIDO_HTML =
  '<b>Negrita</b> y <a href="https://aimma.com.co">enlace</a>.<ul><li>Punto uno</li><li>Punto dos</li></ul>';

describe('Texto rich-text (set:html) == snapshot', () => {
  for (const slug of TEMPLATES) {
    test(`${slug} · html-md-left`, async () => {
      const section = makeSection('texto', {
        contenido: CONTENIDO_HTML,
        alineacion: 'left',
        tamanio: 'md',
      });
      const tienda = makeTienda(slug);
      const html = await renderNormalized(Texto, section, tienda, []);
      await expect(html).toMatchFileSnapshot(
        fileURLToPath(new URL(`./__snapshots__/texto/${slug}__html-md-left.html`, import.meta.url))
      );
    });
  }
});
```

- [ ] **Step 5: Regenerar snapshots y revisar el diff (intencional)**

Run (desde `apps/storefront/`): `npx vitest run test/texto.golden.test.ts -u`
Expected: 
- Se crean 4 snapshots nuevos `__snapshots__/texto/{slug}__html-md-left.html` mostrando los tags renderizados (`<b>`, `<a href="https://aimma.com.co">`, `<ul><li>`).
- Cambian SOLO `editorial_magazine__sm-left.html` y `editorial_magazine__md-left.html`: desaparecen los `<span class="em-dropcap">` y `<span class="sr-only">` (el dropcap pasó a CSS, que no emite el renderToString).
- Los demás snapshots (IC/FB/MA en todos los combos + EM lg/xl/center/right) quedan **idénticos** (contenido plano sin tags → set:html == texto escapado).

Revisar `git diff` de los snapshots byte-a-byte y confirmar que el cambio es exactamente ese (dropcap spans fuera + nuevos snapshots HTML). Si algún snapshot plano IC/FB/MA cambió, investigar antes de seguir.

- [ ] **Step 6: Correr toda la suite del storefront**

Run (desde `apps/storefront/`): `npx vitest run`
Expected: PASS toda la suite (golden + optimized-image). Si `astro check` se usa en CI, correr (desde `apps/storefront/`): `npm run typecheck` y confirmar 0 errores nuevos por los imports nuevos.

- [ ] **Step 7: Commit**

```bash
git add apps/storefront/package.json apps/storefront/src/components/blocks/texto/Texto.astro apps/storefront/test/texto.golden.test.ts apps/storefront/test/__snapshots__/texto package-lock.json
git commit -m "feat(richtext): storefront set:html + dropcap EM por CSS + pin isomorphic-dompurify 2.36.0 + golden"
```

---

## Task 7: Admin — control richText + inspector + section-defs + index.html

**Files:**
- Modify: `iapanel/tienda/admin/views/editor/editor-controls.js`
- Modify: `iapanel/tienda/admin/views/editor/editor-inspector.js`
- Modify: `iapanel/tienda/admin/views/editor/section-defs.js`
- Modify: `iapanel/tienda/admin/index.html`
- Test: `tests/editor/10-richtext-policy-sync.test.mjs` (sub-test de render del control)

- [ ] **Step 1: Escribir el test del control richText (falla primero)**

En `tests/editor/10-richtext-policy-sync.test.mjs`, agregar:

```js
test('richtext: el control renderea un contenteditable con el valor y toolbar', () => {
  const win = bootWindow(['richtext-policy.js', 'editor-controls.js']);
  const C = win.TiendaIA.editorControls;
  let changed = null;
  const node = C.richText('Contenido', '<b>hola</b>', (v) => { changed = v; }, { maxLength: 5000 });
  const editor = node.querySelector('.ed-ctrl__richtext');
  assert.ok(editor, 'no renderizo el contenteditable');
  assert.equal(editor.getAttribute('contenteditable'), 'true');
  assert.ok(node.querySelector('.ed-rt__toolbar'), 'no renderizo la toolbar');
  // En jsdom no hay window.DOMPurify -> normalize cae al fallback (devuelve el valor tal cual).
  assert.ok(editor.innerHTML.includes('hola'), 'no cargo el valor inicial');
});
```

- [ ] **Step 2: Correr el test (falla: no existe C.richText)**

Run (desde `tests/editor/`): `npm test`
Expected: FAIL (`C.richText is not a function`).

- [ ] **Step 3: Implementar el control richText en editor-controls.js**

En `iapanel/tienda/admin/views/editor/editor-controls.js`, antes de la función `slider` (línea 213), agregar:

```js
  // richText (Fase B-controles #4): toolbar + contenteditable -> HTML.
  // Normaliza con DOMPurify (CDN, window.DOMPurify) + la policy del admin para que el WYSIWYG sea
  // honesto (lo que se ve == lo que la EF guardara). La EF es la capa AUTORITATIVA al guardar.
  // No llama nada async en el render; en jsdom (sin DOMPurify) normalize() cae al fallback.
  function richText(label, value, onChange, opts) {
    opts = opts || {};
    const errorEl = el('p', { class: 'ed-ctrl__error', hidden: true });

    function normalize(html) {
      const P = window.TiendaIA && window.TiendaIA.richtextPolicy;
      const DP = window.DOMPurify;
      if (DP && P) return DP.sanitize(html || '', P.toDOMPurify());
      return html || ''; // fallback: la EF sanitiza autoritativamente igual
    }

    const editor = el('div', {
      class: 'ed-ctrl__richtext',
      contenteditable: 'true',
      role: 'textbox',
      'aria-multiline': 'true',
      'aria-label': label,
    });
    editor.innerHTML = normalize(value);

    const fire = debounce(() => {
      const clean = normalize(editor.innerHTML);
      onChange(clean);
    }, DEBOUNCE_MS);

    function cmd(command) {
      document.execCommand(command, false, null);
      editor.focus();
      fire();
    }
    function addLink() {
      const url = window.prompt('URL del enlace (https / mailto / tel):', 'https://');
      if (!url) return;
      if (!/^(https:|mailto:|tel:)/i.test(url)) {
        errorEl.textContent = 'URL no permitida (solo https / mailto / tel)';
        errorEl.hidden = false;
        return;
      }
      errorEl.hidden = true;
      document.execCommand('createLink', false, url);
      editor.focus();
      fire();
    }

    const mkBtn = (txt, title, onClick) => el('button', {
      type: 'button', class: 'ed-rt__btn', title, onClick,
    }, txt);

    const toolbar = el('div', { class: 'ed-rt__toolbar' }, [
      mkBtn('B', 'Negrita', () => cmd('bold')),
      mkBtn('I', 'Itálica', () => cmd('italic')),
      mkBtn('🔗', 'Enlace', addLink),
      mkBtn('• Lista', 'Lista con viñetas', () => cmd('insertUnorderedList')),
      mkBtn('1. Lista', 'Lista numerada', () => cmd('insertOrderedList')),
    ]);

    // Pegar como texto plano: evita traer markup sucio de Word/web (la EF igual sanitiza).
    editor.addEventListener('paste', (e) => {
      e.preventDefault();
      const text = (e.clipboardData || window.clipboardData).getData('text/plain');
      document.execCommand('insertText', false, text);
    });
    editor.addEventListener('input', fire);
    // Al perder foco, normalizar el DOM visible para que coincida con lo que se guardara.
    editor.addEventListener('blur', () => { editor.innerHTML = normalize(editor.innerHTML); });

    return fieldWrapper(label, el('div', { class: 'ed-richtext' }, [toolbar, editor]), errorEl);
  }
```

Y en el objeto exportado `window.TiendaIA.editorControls` (línea 289), agregar `richText` a la lista:

```js
    colorPicker, imagePicker, categoryPicker, richText, slider,
```

- [ ] **Step 4: Correr el test (pasa)**

Run (desde `tests/editor/`): `npm test`
Expected: PASS el render del control + todos los anteriores.

- [ ] **Step 5: Agregar el case 'richtext' en el inspector**

En `iapanel/tienda/admin/views/editor/editor-inspector.js`, en el `switch (campo.control)` de `renderCampo`, después del `case 'textarea':` (línea 160), agregar:

```js
      case 'richtext':
        wrap.appendChild(C.richText(campo.label, p[campo.key] || campo.display_fallback || '',
          v => setProp(ES, sec, campo.key, v, campo), campo.opts || {}));
        break;
```

- [ ] **Step 6: Cambiar el control del campo texto.contenido**

En `iapanel/tienda/admin/views/editor/section-defs.js` (línea 62), cambiar el control del campo `contenido` de `texto`:

```js
        { key: 'contenido', control: 'richtext', label: 'Contenido', default: 'Escribi aqui tu texto.', opts: { maxLength: 5000, rows: 5 } },
```

(solo cambia `control: 'textarea'` → `control: 'richtext'`.)

- [ ] **Step 7: Verificar drift-guard + inspector golden siguen verdes**

Run (desde `tests/editor/`): `npm test`
Expected: PASS. Nota: `03-drift-guard` compara tipos+campos+opcionalidad (cambiar el `control` de un campo existente NO lo afecta — sigue verde por construcción). El golden de inspector (`02-inspector-dom`) del tipo `texto` cambia (textarea→richtext): regenerar con `node capture-golden.mjs` SOLO si 02 falla, y revisar que el diff sea exactamente el campo contenido (textarea→contenteditable+toolbar). Confirmar byte-a-byte.

- [ ] **Step 8: Cablear scripts en index.html (DOMPurify CDN + policy + bumps)**

En `iapanel/tienda/admin/index.html`:

(a) Tras la línea del CDN de supabase (línea 17), agregar el CDN de DOMPurify pineado:

```html
  <script src="https://cdn.jsdelivr.net/npm/dompurify@3.4.7/dist/purify.min.js"></script>
```

(b) Antes de `<script src="views/editor/editor-controls.js?v=6"></script>` (línea 162), agregar el mirror de la policy:

```html
  <script src="views/editor/richtext-policy.js?v=1"></script>
```

(c) Bump de cache-busters de los archivos modificados:
- `section-defs.js?v=4` → `?v=5`
- `editor-controls.js?v=6` → `?v=7`
- `editor-inspector.js?v=8` → `?v=9`

- [ ] **Step 9: Node --check de los JS del admin modificados**

Run (desde la raíz): `node --check iapanel/tienda/admin/views/editor/editor-controls.js && node --check iapanel/tienda/admin/views/editor/richtext-policy.js && node --check iapanel/tienda/admin/views/editor/editor-inspector.js && node --check iapanel/tienda/admin/views/editor/section-defs.js && echo OK`
Expected: `OK` (sin errores de sintaxis).

- [ ] **Step 10: Commit**

```bash
git add iapanel/tienda/admin/views/editor/editor-controls.js iapanel/tienda/admin/views/editor/editor-inspector.js iapanel/tienda/admin/views/editor/section-defs.js iapanel/tienda/admin/index.html tests/editor/10-richtext-policy-sync.test.mjs
git commit -m "feat(richtext): control richText admin (toolbar+contenteditable) + inspector + section-defs + DOMPurify CDN"
```

---

## Task 8: Suite completa verde + audit lap

**Files:** (ninguno nuevo — verificación)

- [ ] **Step 1: Suite tests/editor completa**

Run (desde `tests/editor/`): `npm test`
Expected: PASS 100% (policy-sync 10, security 11, drift-guard 03, image/category/color, load-persistencia, image-sweep, ef-schema-sync, default-props, inspector-dom).

- [ ] **Step 2: Suite storefront completa**

Run (desde `apps/storefront/`): `npx vitest run`
Expected: PASS 100% (goldens incluyendo texto + el nuevo html-md-left).

- [ ] **Step 3: Audit lap (nunca-asumir, paso 5)**

Verificar manualmente:
- `git status` limpio salvo lo committeado; `git log --oneline main..fase-b-richtext` muestra los commits de las tareas 1-7.
- El mirror EF byte-idéntico (sync-test 10 verde) y el deno.json tiene sanitize-html.
- Zod `editor-schema.ts` SIN cambios (drift-guard + ef-schema-sync verdes).
- Golden: solo cambiaron EM sm/md-left + 4 nuevos html-md-left; IC/FB/MA planos idénticos.
- Ninguna otra sección que usa `textarea` (banner.subtitulo, formulario.opciones, video.html) se vio afectada (siguen control 'textarea').

- [ ] **Step 4: Preparar el paquete del gate de deploy (NO deployar)**

Generar para Jorge (sin tocar producción):
- `git diff main..fase-b-richtext` (diff completo).
- Salida de `npm test` de tests/editor mostrando seguridad dual + idempotencia en verde.
- `git diff` de los snapshots golden de texto (byte-a-byte).

Entregar a Jorge y ESPERAR su revisión. El deploy (storefront wrangler + admin Easypanel + EF MCP verify_jwt=TRUE) es Tipo B / gate de Jorge, fuera de este plan.

---

## Self-Review (hecho)

- **Cobertura del spec:** §0 spike (Task incorpora sanitize-html, isomorphic-dompurify descartado) ✓; §2 canónico+adaptadores+sync (Task 1-3) ✓; §3 3 capas (EF Task 5, storefront Task 6, admin Task 7) + idempotencia (Task 4) ✓; §4 Zod sin cambio (audit Task 8) ✓; §5 set:html+dropcap CSS (Task 6) ✓; §6 sync+seguridad dual+idempotencia+golden (Task 1-4,6) ✓; §7 widget (Task 7) ✓; §8 pin (Task 4 tests, Task 6 storefront) ✓.
- **Placeholders:** ninguno; todo el código está completo. Único punto test-driven: el formato void-element en idempotencia (Task 4 Step 4) — config inicial dada (`selfClosing: []`) + resolución documentada.
- **Consistencia de tipos:** `RICHTEXT_POLICY`/`toSanitizeHtml`/`toDOMPurify` con las mismas firmas en canónico (TS), mirror EF (TS idéntico) y admin (JS, value-synced). Versiones pineadas idénticas en EF (deno.json) y tests (package.json) = 2.13.1 / 2.36.0.

## Execution Handoff

Plan completo y guardado en `docs/SUPERPOWERS/plans/2026-06-04-richtext.md`. Dos opciones de ejecución:

1. **Subagent-Driven (recomendada)** — un subagente fresco por tarea, revisión entre tareas, iteración rápida.
2. **Inline Execution** — ejecutar las tareas en esta sesión con checkpoints.

¿Cuál preferís?

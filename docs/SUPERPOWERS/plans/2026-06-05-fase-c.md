# FASE C — Plan de implementación

> **Para workers agénticos:** SUB-SKILL REQUERIDA: superpowers:subagent-driven-development. Steps con checkbox `- [ ]`. Spec: `docs/SUPERPOWERS/specs/2026-06-05-fase-c-design.md`.

**Goal:** Preview en vivo por PATCH de fragmento SSR por-sección (C.1) + click-to-edit en 2 pasos (C.2). C.3 ya hecho (hotfix).

**Arquitectura:** Una sola fuente de render (BlockRenderer) y una sola de validate+sanitize (fn compartida) → cero drift. Dos carriles: tema = CSS-var postMessage instantáneo (existe); contenido/estructura = fragmento SSR swappeado en el iframe. Flujo desacoplado (patch del state local ~300ms; save 1.5s en paralelo).

**Tech stack:** Astro 5 SSR + CF Workers (storefront, endpoint, bridge), Deno (EF save), vanilla JS (admin), Zod + sanitize-html (ambos runtimes), Playwright (verificación runtime).

---

## Cómo quedaron A1–A5 (baked)

- **A1 — Autorización (confused deputy):** RESUELTO en Task 2. El endpoint NO confía `body.tienda_id`. La `tienda` es la del **subdominio** (`Astro.locals.tienda`) y el preview-token se valida contra ESA (`validate_preview_token(token) === locals.tienda.id`, si no → 403). El catálogo/render usa `locals.tienda` (autoritativo del token+subdominio). **El body lleva solo `{ section }`** (se elimina `tienda_id` del contrato). Test en Task 3: token de A contra subdominio B / body.tienda_id=B → 403 / nunca renderiza B.
- **A2 — Orden de deploy en C.1:** RESUELTO en Task 7 (gate). Primero **storefront** (endpoint+bridge, ADITIVO: admin viejo no manda `section-patch` → handler dormido → cero regresión; byte-compare no-regresión como G2/G3). Recién con endpoint vivo+verificado, deploy del **admin** (el caller). El admin nunca llama a un endpoint inexistente.
- **A3 — Paridad incluye PRODUCTOS:** RESUELTO en Task 3 — el golden render==página cubre una sección `productos` (path que carga catálogo por tienda), no solo estáticas.
- **A4 — Bridge adversarial en runtime real:** RESUELTO en Task 7 (gate) — Playwright/chromium estándar G3: sanity positivo + origin malo / shape malo / inyección → rechazados. No solo unit.
- **A5 — Higiene del endpoint:** RESUELTO en Task 2 — token-check PRIMERO (fail-fast, antes de cualquier render/catálogo); errores limpios (status + mensaje corto, sin stack/leak interno).

---

## Task 0: Branch + baseline
- [ ] Confirmar branch `feat/fase-c` (desde main post-hotfix). Suites verdes de arranque: `cd tests/editor && npm test` + `cd apps/storefront && npx vitest run`.

---

## Task 1: Fuente única `validateAndSanitizeSection` (blindaje Q3/req#2)

**Files:**
- Modify: `packages/database/src/editor-schema.ts` (export helper por-sección)
- Create: `packages/database/src/richtext-policy.ts` (mover/compartir la policy sanitize-html que hoy vive en el EF) — o re-export si ya existe shared
- Modify: `supabase/functions/tienda-guardar-layout/index.ts` (usar el helper compartido en `sanitizeHome`)
- Modify: mirrors EF (`editor-schema.ts`, `richtext-policy.ts`) byte-sync
- Test: `tests/editor/15-shared-sanitize.test.mjs`

- [ ] **Step 1 (test first):** test que `validateAndSanitizeSection(rawSection)`:
  - parsea con `SectionSchema` (rechaza inválido),
  - sanitiza `props.contenido` de `texto` con sanitize-html (XSS `<script>`/`onerror` → removido),
  - es idempotente (sanitizar 2x == 1x),
  - y que el `sanitizeHome` del EF produce EXACTAMENTE lo mismo por-sección (misma fn, no duplicada).
- [ ] **Step 2:** correr → falla (fn no existe).
- [ ] **Step 3:** implementar `export function validateAndSanitizeSection(raw): Section` en `packages/database/src/editor-schema.ts` (o módulo hermano): `const s = SectionSchema.parse(raw); if (s.tipo==='texto') s.props.contenido = normalizeVoidEls(sanitizeHtml(s.props.contenido, RICHTEXT_OPTS)); return s;`. Reusa `RICHTEXT_POLICY`/`toSanitizeHtml` (compartir desde packages/database). sanitize-html corre en CF + Deno.
- [ ] **Step 4:** refactor `sanitizeHome` (EF) para que llame `validateAndSanitizeSection` por sección (cero lógica de sanitize duplicada). Re-sync mirrors (cp) + `tests/editor/04-ef-schema-sync` verde.
- [ ] **Step 5:** correr suite → verde. **Commit** `feat(fase-c): validateAndSanitizeSection fuente unica (Zod+sanitize por seccion)`.

---

## Task 2: Endpoint `/_internal/render-section` (storefront) — A1 + A5

**Files:**
- Create: `apps/storefront/src/pages/_internal/render-section.ts` (Astro endpoint, POST)
- Test: `apps/storefront/test/render-section.test.ts`

- [ ] **Step 1 (test first):** tests del endpoint:
  - sin token / token inválido → **403, fail-fast** (no toca catálogo);
  - token de tienda A contra `locals.tienda`=A + `body={section}` válida → 200 con `{html}` que es `<section data-section-id=…>`;
  - **A1**: token de A pero `locals.tienda`=B (mismatch) → 403; `body.tienda_id` (si llega) IGNORADO;
  - error de render → status 5xx con mensaje corto, **sin stack/leak**.
- [ ] **Step 2:** correr → falla.
- [ ] **Step 3:** implementar (orden EXACTO, A5):
  ```ts
  // 1) token PRIMERO (fail-fast)
  const token = url.searchParams.get('preview') || (await req).token...; // mismo mecanismo que la pagina preview
  const tienda = Astro.locals.tienda;            // del subdominio (autoritativo)
  const validId = await supabase.rpc('validate_preview_token', { p_token: token });
  if (!validId || validId !== tienda.id) return clean(403);   // A1: token debe ser de ESTA tienda
  // 2) recien ahora: body + validate/sanitize compartido
  const body = await req.json();
  const section = validateAndSanitizeSection(body.section);    // un source (Task 1)
  // 3) render 1 seccion con BlockRenderer (productos carga catalogo por tienda.id, NO body)
  const html = await renderSectionToString(section, tienda);
  return clean(200, { html });
  ```
  `tienda_id` autoritativo = `tienda.id` (subdominio+token); `body.tienda_id` nunca se usa. Errores via helper `clean(status, body?)` sin exponer internals.
- [ ] **Step 4:** correr → verde. **Commit** `feat(fase-c): endpoint render-section (token-first, tienda del token, fn compartida)`.

---

## Task 3: Golden de paridad render==página (A3)

**Files:** `apps/storefront/test/render-section-parity.test.ts`

- [ ] **Step 1 (test):** para un set de secciones que INCLUYE `productos` (+ banner, texto, imagen): render de página completa (BlockRenderer) vs el `html` del endpoint para cada sección → el `<section data-section-id>` debe ser **byte-idéntico**. Productos: mismo catálogo (mismo tienda fixture).
- [ ] **Step 2:** correr → (ajustar `renderSectionToString` hasta paridad). Verde.
- [ ] **Step 3:** **Commit** `test(fase-c): paridad render-section == pagina (incl. productos)`.

---

## Task 4: Bridge `section-patch` en el storefront (index.astro) — seguridad Q4.3

**Files:** Modify `apps/storefront/src/pages/index.astro` (script preview `isPreview`)

- [ ] **Step 1:** dentro del listener `message` origin-gated existente, agregar handler `section-patch`. **Origin EXACTO** (`e.origin === ADMIN_ORIGIN`; auditar que `theme`/`select` también sean `===`, endurecer si laxo). **Shape estricto ANTES del DOM**:
  ```js
  if (msg.type === 'section-patch') {
    const OPS = { replace:1, insert:1, remove:1, move:1 };
    if (!OPS[msg.op]) return;
    if (typeof msg.sectionId !== 'string' || !/^sec_[a-z0-9]{4,}$/.test(msg.sectionId)) return;
    const cont = document.querySelector('[data-section-id]')?.parentElement;
    const el = document.querySelector('[data-section-id="' + msg.sectionId + '"]');
    if (msg.op === 'remove') { el && el.remove(); return; }
    if (msg.op === 'move') { if (el && cont && Number.isInteger(msg.toIndex)) cont.insertBefore(el, cont.children[msg.toIndex] || null); return; }
    if (typeof msg.html !== 'string') return;
    if (msg.op === 'replace') { el && (el.outerHTML = msg.html); return; }  // html = NUESTRO SSR sanitizado
    if (msg.op === 'insert' && cont && Number.isInteger(msg.index)) {
      const t = document.createElement('template'); t.innerHTML = msg.html.trim();
      const node = t.content.firstElementChild;
      if (node) cont.insertBefore(node, cont.children[msg.index] || null);
    }
  }
  ```
  **Sin re-sanitizar en browser** [Q4.3] (divergiría del publicado + 3er sanitizer). Defensa = fn compartida (origen del html) + origin/shape.
- [ ] **Step 2:** `npm run build` storefront verde. **Commit** `feat(fase-c): bridge section-patch (origin exacto + shape estricto)`.

---

## Task 5: Admin — patch desacoplado + ops sin-fetch + fallback

**Files:** Modify `editor-canvas.js` (applyPatch + fetch fragmento), `editor.js` (debounce ~300ms del patch, paralelo al save), `editor-state.js` (exponer la última op para mapear a patch), `index.html` (cache-busters)

- [ ] **Step 1:** `editor-canvas.js`: `async function renderFragment(section)` → POST a `<tenantOrigin>/_internal/render-section?preview=<token>` con `{section}` → `{html}`. `applyPatch(op, {sectionId, html, index, toIndex})` → `iframe.contentWindow.postMessage({type:'section-patch', op, sectionId, html, index, toIndex}, tenantOrigin)`. En error/timeout o nodo ausente → `reloadFull()` (fallback acotado, re-mint).
- [ ] **Step 2:** `editor.js`: nuevo debounce `schedulePatch(sectionId)` (~300ms) que renderiza el fragmento del **state local** + `applyPatch('replace', …)`. `addSection`→`insert` (render), `duplicate`→`insert`, `remove`→`applyPatch('remove')` (sin fetch), `reorder`→`applyPatch('move', {toIndex})` (sin fetch). undo/redo → `reloadFull()` (primer corte, Q4.2). El `saveDraft` (1.5s) sigue su propio carril. Reemplazar el `editorCanvas.refresh()` post-save por el carril patch (el save ya NO recarga el iframe).
- [ ] **Step 3:** `node --check` los JS + bump cache-busters. **Commit** `feat(fase-c): admin patch desacoplado (~300ms) + ops sin-fetch + fallback`.

---

## Task 6: Tests de swap + desacople + fallback

**Files:** `tests/editor/16-patch-flow.test.mjs`

- [ ] **Step 1:** tests (jsdom/unit): el mapeo op→patch es correcto (reorder=move sin fetch; remove sin fetch; prop/add/dup con fetch); un cambio de **tema** NO dispara `renderFragment` (carriles, mock del fetch); error del fragmento → `reloadFull`. Correr verde. **Commit**.

---

## Task 7: GATE C.1 (deploy por superficie A2 + verificación en vivo A4)

> Checkpoint OBLIGATORIO: OK de Jorge + diff + verificación en vivo. NADA a prod sin OK.

- [ ] **7a — Storefront PRIMERO (aditivo):** `wrangler deploy` (endpoint + bridge). **No-regresión byte-compare** (aimma-test, como G2/G3): el `<html>` público + estructura SSR idénticos (el bridge es preview-only client-side; el endpoint es ruta nueva). Confirmar que el admin viejo (sin section-patch) sigue andando (handler dormido).
- [ ] **7b — Bridge adversarial runtime real (chromium, estándar G3):** sanity positivo (replace válido aplica) + origin malo → ignorado + op fuera de enum → ignorado + sectionId mal formado → ignorado + html con `<img onerror>` (vía el endpoint, confirmando que la fn compartida ya lo limpió) → inerte. + **A1 en vivo:** token de A contra subdominio/tienda B → 403.
- [ ] **7c — Admin DESPUÉS:** merge `--no-ff` a main → Easypanel. Verificación en vivo: editar prop → patch ~instantáneo sin reload; add/remove/reorder → nodo correcto sin reload; cambio de tema → sigue por CSS-var (sin fetch de fragmento); error forzado → fallback reload (re-mint, sin 403). Restaurar aimma-test.

---

## Task 8: C.2 PASO 1 — base inspector + chrome de selección

**Files:** `index.astro` (bridge: barra de sección + etiqueta + outline en select), `editor-canvas.js` (recibir acciones de la barra → editorState ops), inspector reusa rich-text de B-controles #4, `editor-styles.css`, cache-busters.

- [ ] **Step 1:** al `select` (ya existe) → el preview dibuja outline + barra flotante (↑↓ duplicar borrar) + etiqueta del tipo; las acciones postMessage→admin→editorState op→patch (Task 5).
- [ ] **Step 2:** edición de contenido en el inspector (DOM admin), canvas refleja por fragmento. Tests + `node --check`.
- [ ] **Step 3:** **GATE C.2.1** (deploy por superficie + vivo). OK de Jorge.

---

## Task 9: C.2 PASO 2 — inline de texto simple (destino)

**Files:** `index.astro` (contenteditable de texto plano en el preview + postMessage del string), `editor.js`/`editor-state.js` (recibir string → state → render+save).

**INVARIANTE DE RECONCILIACIÓN (nombrar y respetar):** mientras un campo se edita inline, **NINGÚN swap de fragmento toca esa sección** (borraría el cursor). En **blur/commit**, el valor fluye a state local → que alimenta render Y save. Implementar un "lock" de la sección en edición inline (el carril patch la saltea hasta commit).

- [ ] **Step 1:** doble-click en título/subtítulo (texto plano) → contenteditable en el preview; durante la edición la sección queda LOCKED (patch la saltea). blur/Enter → postMessage `{type:'inline-commit', sectionId, field, value}` (string plano, sanitize trivial) → admin actualiza state → unlock → render+save.
- [ ] **GUARDRAIL:** solo texto plano (strings). Rich-text/HTML, imagen, fondo, link, listas, estructura → SIEMPRE inspector. Cero contenteditable de HTML en el storefront.
- [ ] **Step 2:** tests (lock activo durante edición; commit fluye a state+save; guardrail: campos rich-text no son inline). 
- [ ] **Step 3:** **GATE C.2.2** (vivo). **C.2 cierra acá.**

---

## Self-review
- Cobertura spec: C.1 (endpoint/flujo/swap/bridge/fallback/dos-carriles) ✓, C.2 (2 pasos) ✓, seguridad (A1+token-first+fn compartida+bridge) ✓, tests (paridad incl. productos, swap, desacople, fallback, bridge adversarial) ✓.
- A1–A5 baked + reportados arriba.
- Sin placeholders en C.1 (código exacto en endpoint/bridge). C.2 a nivel-task con código clave + invariante de reconciliación nombrado (se detalla al ejecutar su gate).
- Tipos consistentes: `validateAndSanitizeSection`, `applyPatch(op,…)`, mensaje `section-patch`/`inline-commit`.

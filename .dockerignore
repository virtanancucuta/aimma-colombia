.git
.gitignore
README.md
MONOREPO.md
AIMMA-prompts-trabajo.md
*.md
.env
.env.local
.env.example
.vscode
.idea
node_modules
*.log

# Monorepo workspaces — NO se sirven via nginx legacy en Easypanel.
# El storefront vive en Cloudflare Workers/Pages, no en este nginx.
apps/
packages/
package.json
package-lock.json

# Astro / Vite outputs (por si en algun momento se buildea aca por error)
dist/
.astro/
.wrangler/

# Supabase Edge Functions source-of-truth (deploy via MCP, no via nginx)
supabase/

# E2E tests
e2e-tests/

# Backups
keepalive_workflow.json
